
==============================
Basic arithmetic
==============================

return 3 + 3

---

(program
  (return_statement (bin_op (number) op: (op) (number))))

==============================
Basic precedence
==============================

return 3 + 3 * 2

---

(program
  (return_statement (bin_op (number) op: (op) (bin_op (number) op: (op) (number)))))

==============================
A bunch of precedence
==============================

return 4 as string .. 5 as string / 2 + {}

---

(program (return_statement (bin_op
  (type_cast (number) (simple_type))
  op: (op)
  (bin_op
    (bin_op (type_cast (number) (simple_type)) op: (op) (number))
    op: (op)
    (table_constructor)))))
